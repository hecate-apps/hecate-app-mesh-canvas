{erl_opts, [debug_info, warnings_as_errors]}.

{deps, [
    %% Bumped from ~> 0.31.9 to ~> 3.16.0 in the 3.16 fleet bump
    %% (2026-05-06). Repo had not been touched since initial
    %% scaffolding; if/when this app gets real code, it will need
    %% rewriting against the V2 pool API (macula:connect/2 +
    %% macula:publish/4 etc.) since 3.x is wire-incompatible with 0.x.
    {macula, "~> 3.16.0"}
]}.

{shell, [{apps, [hecate_app_mesh_canvas]}]}.
